{
  "name": "@student-care/user-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "build": "tsc -p tsconfig.json && node build.mjs",
    "preview": "node preview.mjs",
    "test": "tsc -p tsconfig.json && node --test dist/pages/visitor.test.js"
  }
}
